services:
  - type: web
    name: tiny-llm
    runtime: python
    plan: free
    buildCommand: pip install -e ".[api]"
    startCommand: tiny-llm-serve
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    healthCheckPath: /api/health
